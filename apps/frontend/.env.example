# Backend API base URL (no trailing slash). Used for HTTP and to derive WebSocket URL.
# In Docker Compose, set to http://backend:8000 for server-side or use the public URL the browser will use.
VITE_API_BASE_URL=http://localhost:8000

# WebSocket audio chunk mode:
# - false (default): browser SpeechRecognition -> text over WS
# - force: always use WS audio chunks (requires backend STT configured)
VITE_WS_AUDIO_STREAMING=false
